# Server-only. Never put this key in the browser or extension.
ELEVENLABS_API_KEY=replace_with_your_api_key
# Run npm run agent:setup to create and save an agent automatically.
ELEVENLABS_AGENT_ID=replace_with_your_agent_id
PORT=3000
